# Production build env vars — picked up by Vite during `npm run build`.
# Do NOT put secrets here; VITE_* variables are bundled into the client.
VITE_API_URL=https://budgetcouple-api.onrender.com/api/v1
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_WEBPUSH=false
VITE_ENABLE_EMAIL_NOTIFICATIONS=false
